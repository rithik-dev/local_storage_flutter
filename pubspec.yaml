name: local_storage_flutter
description: A generalized LocalStorage class with synchronous calls.
version: 0.0.1
homepage: https://github.com/rithik-dev/local_storage_flutter

environment:
  sdk: ">=2.16.1 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

  shared_preferences: ^2.0.15

dev_dependencies:
  flutter_lints: ^1.0.4